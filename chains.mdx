---
title: "Chains"
description: "Base and Robinhood Chain: what is the same, what is different, and why."
icon: "layer-group"
---

One venue, two chains. Same fee hook, same launcher rules, same holder
dividends. Switch in the header; the list, the form and your portfolio
follow.

## Side by side

| | Base | Robinhood Chain |
|---|---|---|
| Chain id | 8453 | 4663 |
| Gas | ETH | ETH |
| Explorer | Basescan | Blockscout |
| Pairs | USDC, USDT, WETH, cbBTC, VIRTUAL, AERO, tokenized stocks, ecosystem tokens, \$BSTONK | **USDG**, WETH, **194 Robinhood stocks** |
| Opening cap | \$500 - \$10,000 (gated: \$250 - \$15,000) | \$250 - \$15,000 |
| Split buy / sell tax, vesting, anti-sniper toll | ✓ | ✓ |
| Holder dividends | ✓ | ✓ |
| Community takeover | ✓ | ✓ |
| \$BSTONK basket wedge | ✓ | - |
| Renounce basket seat at launch | ✓ | - |
| B20 mode | ✓ | - |
| Referrals | ✓ | - |
| Platform fee goes to | \$BSTONK buyback and burn | held as USDG |

## Where the platform's cut goes

<Tabs>
  <Tab title="Base">
    Converted to \$BSTONK and burnt, inside the trade. Every launch on Base burns \$BSTONK just by trading. [The flywheel &rarr;](/bstonk)
  </Tab>
  <Tab title="Robinhood Chain">
    Converted to USDG and **held in the fee vault on 4663**. Nothing bridges automatically. \$BSTONK does not exist on this chain and there is no version of it here.

    This is a decision, not a gap: \$BSTONK stays a Base token, and moving value between chains is a manual act, never a pipe.
  </Tab>
</Tabs>

## Robinhood stocks as pairs

Robinhood issues 194 tokenized equities and ETFs on its chain. Any of
them is launchable against, subject to one gate.

| Rule | |
|---|---|
| Admission | A genuine Robinhood stock token with supply, or USDG or WETH. No allowlist. |
| Gate | Pushing the pair's price down 10x through its deepest market must cost at least **\$1,000** |
| Pricing | Chainlink where a feed exists; the pair's own market otherwise. Read when you sign, never typed. |
| Passing today | About half of the 194. The form only offers pairs that pass. |

<Note>
The gate tests whether a market is real, not whether a price is precise. It exists to stop a launch opening at a market cap that was never there.
</Note>

## Dividends on Robinhood Chain

Paid in the pair - a USDG-paired token pays USDG, an NVDA-paired token
pays NVDA. A [basket](/dividend-baskets) routes them into other admitted
Robinhood assets, as on Base.

## For builders

| | |
|---|---|
| REST API | `chain=robinhood` wherever `chain=base` works. [API &rarr;](/integration/api) |
| Agent API | `chainId: 4663` in the body. [Agents &rarr;](/integration/agents) |
| Contracts | [Robinhood Chain addresses &rarr;](/integration/contracts#robinhood-chain-4663) |
| Launch tuple | No `vaultShareBps`, `issuers` or `renounceBasket`. [On-chain &rarr;](/integration/onchain#robinhood-chain) |

<Warning>
Robinhood Chain makes a block every **0.1s** - 864,000 a day. Filter `eth_getLogs` by address or topic; every public endpoint refuses a wide unfiltered range.
</Warning>
